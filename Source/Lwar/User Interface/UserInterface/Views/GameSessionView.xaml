<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:controls="clr-namespace:Pegasus.UserInterface.Controls;assembly=PegasusUI"
			 xmlns:input="clr-namespace:Pegasus.UserInterface.Input;assembly=PegasusUI"
			 xmlns:userInterface="clr-namespace:Lwar.UserInterface.Views"
			 mc:Ignorable="d"
			 FontFamily="Liberation Sans"
			 FontSize="16"
			 d:DesignHeight="600"
			 d:DesignWidth="800"
			 input:InputManager.AutoFocus="true"
			 Foreground="White">
	<UserControl.InputBindings>
		<input:ConfigurableBinding Cvar="Lwar.Scripting.Cvars.InputChatCvar" Method="ShowChatInput" />
		<input:ConfigurableBinding Cvar="Lwar.Scripting.Cvars.InputShowScoreboardCvar" TriggerMode="Activated" Method="ShowScoreboard" />
		<input:ConfigurableBinding Cvar="Lwar.Scripting.Cvars.InputShowScoreboardCvar" TriggerMode="Deactivated" Method="HideScoreboard" />
		<input:KeyBinding Key="Escape" Method="ShowInGameMenu" TriggerOnRepeat="false" />
		<input:KeyBinding Key="C" Method="ToggleDebugCamera" Modifiers="Control+Shift" TriggerOnRepeat="false" />
	</UserControl.InputBindings>
	<controls:RenderOutputPanel Camera="{Binding CameraManager.ActiveCamera}"
								HasDepthStencil="true"
								DrawMethod="OnDraw"
								input:Cursor.Cursor="Lwar.Assets.GameBundle.Crosshair"
								ResolutionSource="{Binding ResolutionSource}">
		<controls:AreaPanel x:Name="LayoutRoot">

			<userInterface:ShipOverlayPanel />

			<Grid VerticalAlignment="Bottom" HorizontalAlignment="Right" Background="#AA000000">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="100" />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
				</Grid.RowDefinitions>

				<TextBlock Text="Weapon 1" Grid.Row="0" Grid.Column="0" Margin="5,5,0,0" />
				<TextBlock Text="{Binding WeaponEnergy1}" Grid.Row="0" Grid.Column="1" Margin="0,5,5,0" />

				<TextBlock Text="Weapon 2" Grid.Row="1" Grid.Column="0" Margin="5,2,0,0" />
				<TextBlock Text="{Binding WeaponEnergy2}" Grid.Row="1" Grid.Column="1" Margin="0,2,5,0" />

				<TextBlock Text="Weapon 3" Grid.Row="2" Grid.Column="0" Margin="5,2,0,0" />
				<TextBlock Text="{Binding WeaponEnergy3}" Grid.Row="2" Grid.Column="1" Margin="0,2,5,0" />

				<TextBlock Text="Weapon 4" Grid.Row="3" Grid.Column="0" Margin="5,2,0,5" />
				<TextBlock Text="{Binding WeaponEnergy4}" Grid.Row="3" Grid.Column="1" Margin="0,2,5,5" />
			</Grid>
			
			<userInterface:RespawnView DataContext="{Binding Respawn}" />
			<userInterface:WaitingView />
			<userInterface:EventListView />
			<userInterface:ScoreboardView DataContext="{Binding Scoreboard}" />
			<userInterface:ChatView DataContext="{Binding Chat}" />

		</controls:AreaPanel>
	</controls:RenderOutputPanel>

</UserControl>